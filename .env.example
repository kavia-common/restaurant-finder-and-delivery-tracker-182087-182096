# Environment configuration for Food Delivery Frontend (Next.js)
# Copy this file to `.env` and fill in the values. Do NOT commit your `.env`.

# PUBLIC_INTERFACE
# NEXT_PUBLIC_API_BASE_URL: Base URL for the backend REST API that the frontend will call.
# Example: https://api.example.com or http://localhost:4000
NEXT_PUBLIC_API_BASE_URL=

# PUBLIC_INTERFACE
# NEXT_PUBLIC_WS_URL: WebSocket endpoint for real-time order updates.
# Example: wss://api.example.com/ws or ws://localhost:4000/ws
NEXT_PUBLIC_WS_URL=

# PUBLIC_INTERFACE
# NEXT_PUBLIC_MAPS_KEY: Public maps provider key for map-related components.
# Do not use secrets here; this is a public client-side key.
NEXT_PUBLIC_MAPS_KEY=
